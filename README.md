# Stand up rand up

Selects a random word from a list

e.g.

```bash
curl http://localhost:3000/?names=piglet,george,pup
```

# development

install deps:

```bash
npm i
```

run:

```bash
npm start
```
